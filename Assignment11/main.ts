// Define an array of names
const names: string[] = ["Alice", "Bob", "Charlie", "David", "Eve"];

// Print each person's name by accessing each element in the list using a loop
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}
