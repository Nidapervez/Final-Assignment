"use strict";
//favorite mode of transportation
const favoritetransportation = ["motorcycle", "car", "bicycle", "scooter", "electric skateboard"];
//Print each transportation's name by accessing each element with series of statements
for (let i = 0; i < favoritetransportation.length; i++) {
    console.log("I Would like to own", favoritetransportation[i]);
}
